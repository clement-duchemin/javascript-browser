// exo 1


/* let button = document.getElementById('make-thing-appear');
let thing = document.getElementById('thing-appear');

button.addEventListener('click', () => {
    thing.style.opacity = 1
});
 */


// exo 2

/* let square = document.getElementById('square');

square.addEventListener('click', () => {
    square.style.transform = 'translate(42px, 18px)';
}) */

// exo 3

/* function demarrerAnimation() {
    monCercle.style.animation = 'rotation 5s linear infinite';
  }
  
  const monCercle = document.getElementById('monCercle');
  monCercle.addEventListener('click', demarrerAnimation); */
  
  // exo 4


  /* let star = document.getElementById('etoile');
  
  let colors = ['red', 'blue', 'yellow'];
  let currentColorIndex = 0;
  
  setInterval(function() {
    currentColorIndex = (currentColorIndex + 1) % colors.length;
    star.style.backgroundColor = colors[currentColorIndex];
  }, 3000); */

// exo 5

/* function startAnimation() {
    monCercle.style.animation = 'growing 5s linear 2';
  }
  
  const monCercle = document.getElementById('rectangle');
  monCercle.addEventListener('click', startAnimation);  */


  //exo 6 

 /*  let monImage = document.getElementById('monImage');

  window.addEventListener('scroll', function() {
    let scrollTop = window.scrollY;  // cette propriété me donne le nombre de pixels qui ont été scrollé verticalement
    let imageTop = monImage.offsetTop; // cete propriété me renvoie la distance entre le haut de l'image et de la page

    if(scrollTop > imageTop - window.innerHeight){
      monImage.style.opacity = "1";
    }
  }); */

  